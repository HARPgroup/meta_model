#!/bin/csh

set scenario = $1
set luseg = $2
set tempdir = $3
# parse
set split=($luseg:as/_/ /)
set lu = $split[1]
set seg = $split[2]
set tree = $MODEL_ROOT

export scenario luseg tempdir lu seg tree
