# How to Add a Model
- create a folder with the model name in subdirectory "models" with the following scripts
  - get_work_path: returns a working directory if none supplied to model
  - model_config: sets all necessary model runtime variables
  - Basic shell can be created by: scripts/make_model_shell
